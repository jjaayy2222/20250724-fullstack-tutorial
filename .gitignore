# -----------------------------------------------------
# 1. Python (Django 백엔드 관련)
# -----------------------------------------------------
# 가상 환경 (Virtual Environments)
.venv/
venv/
fs_env/
lc_env/

# Python 컴파일된 바이트코드 및 캐시 파일
__pycache__/
*.pyc
*.pyo

# 데이터베이스 파일
*.sqlite3
db.sqlite3
example_project/db.sqlite3

# Django 로컬 설정 파일 (민감 정보 포함 가능)
local_settings.py

# 미디어 파일 (업로드된 사용자 파일)
media/

# 기타 Python 로그 파일
*.log
*.pot

# -----------------------------------------------------
# 2. Node.js (Next.js 프론트엔드 관련)
# -----------------------------------------------------
# Node.js 패키지 및 빌드 아티팩트
# 프로젝트 루트 또는 앱 내 모든 node_modules 무시
node_modules/
# (선택 사항) 명시적으로 frontend 내부 node_modules 무시                   
frontend/node_modules/
# Next.js 빌드 아티팩트          
frontend/.next/
# Next.js export 결과물 (정적 사이트 생성 시)                
frontend/out/                   

# Node.js 로그 및 캐시 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tgz # npm 압축 파일 (tarball)


# -----------------------------------------------------
# 3. 환경 변수 파일 (공통 및 백엔드)
# -----------------------------------------------------
# 민감 정보가 포함될 수 있는 환경 변수 파일
.env

backend/.env

frontend/.env
frontend/.env.local
frontend/.env.*.local

# -----------------------------------------------------
# 4. 운영체제 및 IDE 관련 파일
# -----------------------------------------------------
# macOS 시스템 파일 (숨김 파일)
.DS_Store

# IDE 또는 편집기 스왑/설정 파일
*.swp
# JetBrains IDE (IntelliJ, PyCharm 등)
.idea/
# VS Code 설정 (개인 설정이 아니라면 보통 무시)              
.vscode/            
*.code-workspace
# IntelliJ 모듈 파일
*.iml               


# -----------------------------------------------------
# 5. 기타 임시 파일 및 캐시
# -----------------------------------------------------
# Emacs/Vim 백업 파일 등 임시 파일
*~
# pytest 캐시 디렉토리                  
.pytest_cache/
# coverage.py 보고서 파일      
.coverage
# coverage.py 보고서 디렉토리           
htmlcov/            

# requirements.txt 
temp/