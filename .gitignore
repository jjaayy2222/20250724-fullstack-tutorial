# .gitignore

# -----------------------------------------------
# 1. Python (Django 백엔드 관련)
# -----------------------------------------------
# 가상 환경 (Virtual Environments)
# .venv/ 와 venv/ 중 하나만 사용하는 것이 일반적. 둘 다 포함.
.venv/
venv/

# Python 컴파일된 바이트코드 및 캐시 파일
__pycache__/
*.pyc
*.pyo 
# 최적화된 바이트코드 (선택 사항)

# 데이터베이스 파일
# db.sqlite3는 특정 파일 이름이고, *.sqlite3는 모든 .sqlite3 파일을 포함.
# 일반적으로 *.sqlite3만 있어도 충분.
*.sqlite3

# Django 로컬 설정 파일 (민감 정보 포함 가능)
local_settings.py

# 미디어 파일 (업로드된 사용자 파일)
# MEDIA_ROOT 경로에 따라 다를 수 있지만, 일반적으로 media/ 폴더를 무시.
media/

# 기타 Django/Python 로그 파일
*.log
*.pot


# -----------------------------------------------
# 2. Node.js (Next.js 프론트엔드 관련)
# -----------------------------------------------
# Node.js 패키지 및 빌드 아티팩트
frontend/node_modules/
frontend/.next/

# Node.js 로그 및 캐시 파일
frontend/npm-debug.log* 
# *.log도 포함하지만, npm-debug는 명시적으로
frontend/*.tgz          
# npm 압축 파일

# 환경 변수 파일 (민감 정보 포함 가능)
frontend/.env.local
frontend/.env.*.local
frontend/.env           
# 일반적인 .env 파일 (아래 '기타' 섹션과 겹침)


# -----------------------------------------------
# 3. 운영체제 및 IDE 관련 파일
# -----------------------------------------------
# macOS 시스템 파일 (숨김 파일)
.DS_Store

# IDE 또는 편집기 스왑 파일
*.swp
.idea/          
# IntelliJ/PyCharm 등 JetBrains IDE (선택 사항)
.vscode/        
# VS Code 설정 (선택 사항, 개인 설정이 아니라면 제외 가능)
*.iml           
# IntelliJ 모듈 파일


# -----------------------------------------------
# 4. 기타 자주 무시되는 파일 및 변경사항
# -----------------------------------------------
# 일반적인 로그 파일 (다른 섹션에서 이미 포함된 경우가 많음)
# *.log 는 위에서 이미 포함되었으므로 여기서는 삭제.

# 임시 파일
*~ 
# Emacs/Vim 백업 파일 등

# 민감 정보가 포함될 수 있는 환경 변수 파일 (프론트엔드에 없는 경우)
# 위 frontend/.env 와 겹치므로 만약 백엔드 루트에도 .env가 있다면 여기에 추가
.env


# -----------------------------------------------
# 5. 수동으로 삭제하거나 Git에 커밋하지 않을 파일/폴더 (변경 사항)
# -----------------------------------------------
